# Test Environment Configuration
# Copy this to .env.test in your project root

# Test Database Configuration
DATABASE_URL="postgresql://destino_test:[PASSWORD]@[YOUR-VPS-IP]:5433/destino_sf_test"
DIRECT_URL="postgresql://destino_test:[PASSWORD]@[YOUR-VPS-IP]:5433/destino_sf_test"

# Test Environment Flag
NODE_ENV=test

# Disable external services in tests
SKIP_ENV_VALIDATION=true
DISABLE_EMAILS=true
DISABLE_PAYMENTS=true

# Test-specific configurations
TEST_TIMEOUT=30000
JEST_TIMEOUT=30000

# Mock external service URLs
NEXT_PUBLIC_SUPABASE_URL="http://localhost:3000/mock-supabase"
NEXT_PUBLIC_SUPABASE_ANON_KEY="mock-key-for-testing"

# Instructions:
# 1. Replace [PASSWORD] with your actual database password
# 2. Replace [YOUR-VPS-IP] with your Coolify VPS IP address
# 3. Save this as .env.test in your project root
# 4. Add .env.test to your .gitignore file
